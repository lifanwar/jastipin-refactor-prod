generate text:
```text
python manage.py makemessages -l id --ignore="venv/*" --settings config.settings.dev
```

compile text:
```text
python manage.py compilemessages --settings config.settings.dev
```